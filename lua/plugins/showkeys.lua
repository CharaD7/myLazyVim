return {
  "siduck/showkeys",
  cmd = "ShowkeysToggle",
  opts = {
    exclude_modes = {"i"},
    maxkeys = 3,
    position = 'bottom-center',
    show_count = true,
    timeout = 1,
    -- more opts
  }
}
